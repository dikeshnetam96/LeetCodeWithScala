package leetcode

object PascalTriangle extends App {
/*
  val n : Int = 4

  def process(n: Int, value: List[Int],res : List[Int], s: Int, e: Int) : List[Int] = {
    if(value.size<e) value.concat(List(1))
    else{
    process(n,value,res.concat(List(value(s)+value(e))),s+1,e+1)
    }
  }

  def helper(num : Int, acc : List[List[Int]]) : List[List[Int]] = {

    val list = List(1)
    val computedList = process(num,list,List(1),0,1)
    computedList
  }

  def pascalTriagleHelper(num : Int) : List[List[Int]] = {
  if(num==1) {
    val list : List[List[Int]] = List(List(1))
  }
  else{

  }*/
}
